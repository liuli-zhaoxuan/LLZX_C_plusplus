//TODO:持久 worker（长存线程）+ 主线程不阻塞地提交耗时 I/O 任务并在主线程处理回调
/*
核心组件：
任务队列（线程安全）
持久Worker线程(condition_variable避免忙等)
任务提交接口（post）+ 异步结果
主线程回调队列（将结果写回给主线程）：这里两种方法future/promise（调用方主动等待或轮询）和回调（callback）/完成队列
*/


//升级场景：主线程会不断、随机地提交带不同延时（delay）的任务到长期存在的工作线程上执行。
/*
优先队列（min-heap）
*/

